import { Segment } from "./Sequencer";

export interface TimeConfig {
    
}
export class TimeSegment implements Segment {
    constructor(config: TimeConfig) {

    }

    intialize():void {
        
    }
}